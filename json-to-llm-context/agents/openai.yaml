interface:
  display_name: "JSON to LLM Context"
  short_description: "Compress JSON into readable LLM context"
  default_prompt: "Use $json-to-llm-context to turn this JSON into compact readable context."
